{
  "name": "motion-audit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "G-code travel/retraction auditor for extrusion 3D printers",
  "bin": {
    "motion-audit": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
